# Legacy scripts

The original standalone scripts that Cropsmith grew out of. They are kept here
for reference. The maintained, cross-platform tooling now lives in the
`cropsmith/` package and is exposed through the `cropsmith` CLI.

Note: several of these (e.g. `ScreenRecorder.py`) hardcode Windows paths.
